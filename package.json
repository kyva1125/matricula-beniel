{
  "name": "matricula-beniel-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "description": "Monorepo para el sistema de Matrícula Beniel",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "clean": "rimraf node_modules **/node_modules **/dist dist"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "turbo": "^2.0.0"
  }
}
